\documentclass[tikz, border=10pt]{standalone}
\usepackage{amsmath}
\usetikzlibrary{shapes.geometric, arrows, positioning}

\begin{document}

% Define block styles
\tikzstyle{startstop} = [rectangle, rounded corners, minimum width=3cm, minimum height=1cm,text centered, draw=black, fill=gray!10]
\tikzstyle{process} = [rectangle, minimum width=3.5cm, minimum height=1cm, text centered, draw=black, fill=blue!10]
\tikzstyle{decision} = [diamond, aspect=2, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=green!10]
\tikzstyle{arrow} = [thick,->,>=stealth]
\tikzstyle{data} = [trapezium, trapezium left angle=70, trapezium right angle=110, minimum width=3.5cm, minimum height=1cm, text centered, draw=black, fill=orange!10]

\begin{tikzpicture}[node distance=2cm]

% Nodes
\node (start) [startstop] {Start};
\node (input) [data, below of=start] {Input Data $X$};
\node (preprocess) [process, below of=input] {1. Data Preprocessing};
\node (density) [process, below of=preprocess] {2. Local Density Estimation};
\node (parameters) [process, below of=density] {3. Adaptive Parameter Calculation};
\node (initial_clusters) [process, below of=parameters, yshift=-0.5cm] {4. Form Initial Clusters};
\node (multi_scale) [process, below of=initial_clusters] {5. Multi-Scale Clustering};
\node (stability) [process, below of=multi_scale] {6. Cluster Stability Analysis};
\node (select_clusters) [process, below of=stability] {7. Select Stable Clusters};
\node (postprocess) [process, below of=select_clusters] {8. Post-processing};
\node (output) [data, below of=postprocess] {Output: Cluster Labels};
\node (end) [startstop, below of=output] {End};

% Arrows
\draw [arrow] (start) -- (input);
\draw [arrow] (input) -- (preprocess);
\draw [arrow] (preprocess) -- (density);
\draw [arrow] (density) -- (parameters);
\draw [arrow] (parameters) -- (initial_clusters);
\draw [arrow] (initial_clusters) -- (multi_scale);
\draw [arrow] (multi_scale) -- (stability);
\draw [arrow] (stability) -- (select_clusters);
\draw [arrow] (select_clusters) -- (postprocess);
\draw [arrow] (postprocess) -- (output);
\draw [arrow] (output) -- (end);

\end{tikzpicture}

\end{document}
